name: Deploy to Railway

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Railway Deployment
        uses: nikaro/actions-railway-deploy@v1
        with:
          railway_token: ${{ secrets.RAILWAY_TOKEN }}